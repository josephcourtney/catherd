# Immediate work

The public-CLI hardening pass is implemented.

- Run `just check` on the current tree.
- Fix any remaining formatting, lint, type, import, shell-integration, or pytest failures.
- Once the gate passes, update STATUS.md to record the new verified baseline before further maintenance or release work.
